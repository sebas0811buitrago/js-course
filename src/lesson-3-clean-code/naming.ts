// =====variables

// holds info about a user
const u = {
  age: "18",
  name: "peranito",
  email: "peranito@gmail.com",
};

const validateUserLoginData = () => {
  return true;
};

// holds boolean of login validation
const v = validateUserLoginData();

// ====functions

// saves user data to a database
const process = () => {};

// validate user input
const val = () => {};

// Classes are instantiated so the name should make sense

// User

class UEntity {}

class UserObject {}
